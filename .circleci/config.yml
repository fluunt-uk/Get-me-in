version: 2
jobs:
  build:
    docker:
      # specify the version
      - image: circleci/golang:1.9

      # Specify internal dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    #### TEMPLATE_NOTE: go expects specific checkout path representing url
    #### expecting it in the form of
    ####   /go/src/github.com/circleci/go-tool
    ####   /go/src/bitbucket.org/circleci/go-tool
    working_directory: /go/src/github.com/ProjectReferral/Get-me-in
    steps:
      - checkout
#       - run:
#           name: post checkout check
#           command: |
#             echo $PWD
#             ls -aslh .
#             ls -aslh ~
#             ls -aslh ~/tmp

      # specify any bash command here prefixed with `run: `
      - run: go get -v -t -d ./account-api/...
      - run: go build -v ./account-api/cmd
      - run: go get -v -t -d ./marketing-api/...
      - run: go build -v ./marketing-api/cmd
      - run: go get -v -t -d ./auth-api/...
      - run: go build -v ./auth-api/cmd
